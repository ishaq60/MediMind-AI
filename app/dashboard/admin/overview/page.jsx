"use client"
import React from 'react';
import Loading from '../../loading';

const page = () => {
    <Loading></Loading>
    return (
        <div>
            
        </div>
    );
};

export default page;