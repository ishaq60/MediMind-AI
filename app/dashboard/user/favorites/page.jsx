"iuse client"
import React from 'react';
import Loading from '../../loading';

const page = () => {
    return (
        <div>
            <Loading></Loading>
        </div>
    );
};

export default page;